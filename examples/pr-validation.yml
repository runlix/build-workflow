name: PR Validation

on:
  pull_request:
    branches:
      - release
    types: [opened, synchronize, reopened]
    paths:
      - ".ci/config.json"
      - ".ci/*.sh"
      - "linux-*.Dockerfile"
      - ".github/workflows/pr-validation.yml"
      - ".github/workflows/release.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: validate
    # Pin to a merged full SHA from runlix/build-workflow.
    uses: runlix/build-workflow/.github/workflows/pr-validation-v2.yml@YOUR_BUILD_WORKFLOW_SHA
