name: update-digests-automated

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: update-digests-automated
      cancel-in-progress: false

    steps:
      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Update digests across repositories
        env:
          OWNER: ${{ github.repository_owner }}
          GH_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          set -Eeo pipefail

          echo "🔍 Discovering repositories with docker-image topic..."
          REPOSITORIES=$(gh repo list "$OWNER" --limit 200 --json name,repositoryTopics --jq '.[] | select([.repositoryTopics[]?.name] | index("docker-image")) | .name')
          
          echo "🔍 Found repositories: $REPOSITORIES"
          for REPO in $REPOSITORIES; do
            echo "📦 Processing $OWNER/$REPO"

            gh api "/repos/$OWNER/$REPO/branches" \
              --jq '.[] | select(.name!="main" and .name!="master") | .name' |
            while read -r BRANCH; do
              echo "➡️ Branch $BRANCH"
              
              DIR=$(mktemp -d)
              git clone --depth 1 -b "$BRANCH" "https://${GH_TOKEN}@github.com/$OWNER/$REPO.git" "$DIR"
              cd "$DIR"

              [[ -x update-digests.sh ]] || { echo "⏭ No script"; cd ..; rm -rf "$DIR"; continue; }
              ./update-digests.sh || { echo "❌ Script failed"; cd ..; rm -rf "$DIR"; continue; }

              git add .
              # Check if there are changes to commit
              if git diff --cached --quiet; then
                echo "✔️ No changes"
                cd ..; rm -rf "$DIR"; continue
              fi

              COMMIT_MSG="Upstream image update"
              [[ "$BRANCH" == "pr" ]] && COMMIT_MSG="Upstream image update [skip ci]"
              
              PR_BRANCH="update-digests-${GITHUB_RUN_ID}-$(date +%s)"
              git checkout -b "${PR_BRANCH}" || { echo "❌ Failed to create branch"; cd ..; rm -rf "$DIR"; continue; }
              git commit -m "$COMMIT_MSG" || { echo "❌ Failed to commit"; cd ..; rm -rf "$DIR"; continue; }
              git push -u origin "${PR_BRANCH}" || { echo "❌ Failed to push"; cd ..; rm -rf "$DIR"; continue; }
              
              gh pr create \
                --repo "$OWNER/$REPO" \
                --title "update-digests" \
                --body "Automated digest update for branch \`${BRANCH}\`" \
                --base "${BRANCH}" \
                --head "${PR_BRANCH}" \
                --label "automated,dependencies" || true

              cd ..; rm -rf "$DIR"
            done
          done
