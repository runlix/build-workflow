name: CI PR Validation

on:
  workflow_call:
    inputs:
      build-workflow-ref:
        description: Full commit SHA for the build-workflow revision to load internal actions from.
        required: true
        type: string
      config-path:
        description: Path to the CI config file in the caller repository.
        required: false
        default: .ci/config.json
        type: string
      context-dir:
        description: Docker build context directory in the caller repository.
        required: false
        default: .
        type: string

concurrency:
  group: pr-validation-${{ github.repository }}-${{ github.ref }}-${{ inputs.config-path }}
  cancel-in-progress: true

jobs:
  plan:
    name: plan
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.plan.outputs.matrix }}
      short_sha: ${{ steps.context.outputs.short_sha }}
      image_name: ${{ steps.validate.outputs.image_name }}
      version: ${{ steps.validate.outputs.version }}
      enabled_count: ${{ steps.validate.outputs.enabled_count }}
      manifest_tags: ${{ steps.validate.outputs.manifest_tags }}
    steps:
      - name: Checkout service repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Validate build-workflow ref
        shell: bash
        env:
          BUILD_WORKFLOW_REF: ${{ inputs.build-workflow-ref }}
        run: |
          set -euo pipefail

          [[ "$BUILD_WORKFLOW_REF" =~ ^[a-f0-9]{40}$ ]] || {
            echo "build-workflow-ref must be a full 40-character commit SHA" >&2
            exit 1
          }

      - name: Checkout build-workflow tooling
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          repository: runlix/build-workflow
          ref: ${{ inputs.build-workflow-ref }}
          path: .build-workflow

      - name: Resolve source context
        id: context
        shell: bash
        run: |
          set -euo pipefail
          printf 'short_sha=%s\n' "${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Validate CI config
        id: validate
        uses: ./.build-workflow/.github/actions/internal/ci/validate-config
        with:
          config-path: ${{ inputs.config-path }}

      - name: Plan build matrix
        id: plan
        uses: ./.build-workflow/.github/actions/internal/ci/plan-matrix
        with:
          config-path: ${{ inputs.config-path }}
          short-sha: ${{ steps.context.outputs.short_sha }}

  build:
    name: build
    permissions:
      contents: read
    needs: plan
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.plan.outputs.matrix) }}
    steps:
      - name: Checkout service repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Validate build-workflow ref
        shell: bash
        env:
          BUILD_WORKFLOW_REF: ${{ inputs.build-workflow-ref }}
        run: |
          set -euo pipefail

          [[ "$BUILD_WORKFLOW_REF" =~ ^[a-f0-9]{40}$ ]] || {
            echo "build-workflow-ref must be a full 40-character commit SHA" >&2
            exit 1
          }

      - name: Checkout build-workflow tooling
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          repository: runlix/build-workflow
          ref: ${{ inputs.build-workflow-ref }}
          path: .build-workflow

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

      - name: Build target
        id: build-target
        uses: ./.build-workflow/.github/actions/internal/ci/build-target
        with:
          config-path: ${{ inputs.config-path }}
          target-name: ${{ matrix.name }}
          mode: pr
          short-sha: ${{ needs.plan.outputs.short_sha }}
          context-dir: ${{ inputs.context-dir }}

      - name: Run target test
        if: ${{ matrix.test != '' }}
        timeout-minutes: 10
        shell: bash
        env:
          IMAGE_TAG: ${{ steps.build-target.outputs.image_tag }}
          PLATFORM: ${{ steps.build-target.outputs.platform }}
          TEST_SCRIPT: ${{ matrix.test }}
        run: |
          set -euo pipefail

          echo "Running test script: $TEST_SCRIPT"
          chmod +x "$TEST_SCRIPT"
          "$TEST_SCRIPT"

  summary:
    name: summary
    permissions:
      contents: read
    needs:
      - plan
      - build
    if: ${{ always() }}
    runs-on: ubuntu-24.04
    steps:
      - name: Generate validation summary
        shell: bash
        env:
          PLAN_RESULT: ${{ needs.plan.result }}
          BUILD_RESULT: ${{ needs.build.result }}
          SHORT_SHA: ${{ needs.plan.outputs.short_sha }}
          IMAGE_NAME: ${{ needs.plan.outputs.image_name }}
          MATRIX_JSON: ${{ needs.plan.outputs.matrix }}
          REPOSITORY: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail

          {
            echo "## PR Validation Summary"
            echo ""
            echo "- Repository: \`$REPOSITORY\`"
            echo "- Commit: \`$SHORT_SHA\`"
            echo "- Plan result: \`$PLAN_RESULT\`"
            echo "- Build result: \`$BUILD_RESULT\`"
            echo ""
          } >> "$GITHUB_STEP_SUMMARY"

          if [ "$PLAN_RESULT" = "success" ] && [ -n "${MATRIX_JSON:-}" ]; then
            target_count="$(jq 'length' <<< "$MATRIX_JSON")"
            manifest_tags="$(jq -r '[.[].tag] | unique | join(", ")' <<< "$MATRIX_JSON")"

            {
              echo "### Build Plan"
              echo ""
              echo "- Image: \`$IMAGE_NAME\`"
              echo "- Enabled targets: \`$target_count\`"
              echo "- Manifest tags: \`$manifest_tags\`"
              echo ""
              echo "| Target | Arch | Tag | Dockerfile | Test |"
              echo "| --- | --- | --- | --- | --- |"
              jq -r '
                .[]
                | "| \(.name) | \(.arch) | \(.tag) | \(.dockerfile) | "
                  + (if (.test // "") == "" then "no" else "yes" end)
                  + " |"
              ' <<< "$MATRIX_JSON"
              echo ""
            } >> "$GITHUB_STEP_SUMMARY"
          fi

          echo "[View workflow run]($RUN_URL)" >> "$GITHUB_STEP_SUMMARY"

          if [ "$PLAN_RESULT" != "success" ]; then
            echo "Validation or matrix planning failed." >&2
            exit 1
          fi

          if [ "$BUILD_RESULT" != "success" ]; then
            echo "One or more target builds or tests failed." >&2
            exit 1
          fi
